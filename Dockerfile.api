FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Build the Next.js app (API is served via Next.js Route Handlers)
RUN npm run build

EXPOSE 3002

# Run DB migrations then start the production Next.js server
CMD ["sh", "-c", "node server/scripts/migrate.js && npm start"]
